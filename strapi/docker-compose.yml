services:
  postgres:
    container_name: won_games_db
    image: postgres:14-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: won_admin
      POSTGRES_PASSWORD: wonadmin
      POSTGRES_DB: won_games
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -u won_admin -d won_games"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: